import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

/* Rendering the App component to the root element in the HTML file. */
ReactDOM.render(
<React.StrictMode>
	<App />
</React.StrictMode>,
document.getElementById('root')
);
